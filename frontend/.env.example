# Backend API origin (absolute URL WITHOUT the /api/v1 suffix unless you include it in App.jsx's VITE_API_URL).
# Set this when the frontend and backend are hosted separately (e.g. frontend on Vercel, backend on Render).
VITE_API_URL=https://geo-auditor-szbe.onrender.com/api/v1
