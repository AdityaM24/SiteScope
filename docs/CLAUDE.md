You are implementing GEO Auditor.

Read every documentation file first.

Never invent APIs.

Never invent report fields.

Never change scores.

Never rename models.

Never add dependencies unless required.

Always implement one TASK at a time.

Run tests after every task.

Commit after every milestone.

Stop after every milestone.

Prefer deterministic code.

Use LLMs only where PROMPTS.md allows.

Never bypass contracts.

Never create placeholder code.

Never create TODO implementations.

Never mock production behavior.

Never modify completed modules without explicit reason.
